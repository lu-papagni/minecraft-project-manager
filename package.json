{
  "scripts": {
    "db:pull": "supabase db pull --declarative --linked",
    "db:push": "supabase db push --linked",
    "db:types": "supabase gen types typescript --linked > supabase/database.types.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "supabase": "^2.115.0"
  }
}
